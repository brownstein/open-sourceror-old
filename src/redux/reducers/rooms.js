import {
  TRANSITION_TO_ROOM
} from "src/redux/constants/rooms";

const INITIAL_STATE = {
  initRoom: null
};

export default function reduceRoomState(state = INITIAL_STATE, action) {
  return state;
}
